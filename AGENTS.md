## Agent skills

### Issue tracker

Issues and specs live as markdown files under `.scratch/<feature-slug>/`. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: a root `CONTEXT.md` + `docs/adr/`, created lazily as needed. See `docs/agents/domain.md`.
